# Firebase Environment Variables - Quick Reference

## 📋 Copy-Paste Template for Vercel

Add these in **Vercel Dashboard → Settings → Environment Variables**:

```
VITE_FIREBASE_API_KEY=[Get from Firebase Console]
VITE_FIREBASE_AUTH_DOMAIN=planted-dea3b.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=planted-dea3b
VITE_FIREBASE_STORAGE_BUCKET=planted-dea3b.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=[Get from Firebase Console]
VITE_FIREBASE_APP_ID=[Get from Firebase Console]
VITE_FIREBASE_MEASUREMENT_ID=[Get from Firebase Console]
```

## 🔗 Direct Links

- **Firebase Console**: https://console.firebase.google.com/project/planted-dea3b/settings/general
- **Vercel Environment Variables**: https://vercel.com/settings/environment-variables
- **Your Live Site**: https://planted-ashy.vercel.app/

## ⚡ Quick Steps

1. **Firebase Console** → Copy config values
2. **Vercel** → Add as environment variables (with `VITE_` prefix)
3. **Select all 3 environments** (Production, Preview, Development)
4. **Redeploy**
5. **Done!** ✅

## 🎯 What to Expect

**Console output after Firebase is configured:**
```
✅ Firebase initialized successfully
```

**Instead of:**
```
⚠️ Firebase not configured - user features disabled
```

## 📝 Notes

- All variables MUST start with `VITE_` (Vite requirement)
- Select all environments when adding
- Redeploy required after adding variables
- Takes ~2 minutes to rebuild and go live
